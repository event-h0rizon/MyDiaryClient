
    const currentTimeInMilliseconds = Date.now();
    const istTimeString = new Date(currentTimeInMilliseconds).toLocaleString('en-IN', {
      timeZone: 'Asia/Kolkata', // IST timezone
    });
